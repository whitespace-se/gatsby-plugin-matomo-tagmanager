{
  "name": "gatsby-plugin-matomo-tagmanager",
  "description": "Gatsby plugin to add matomo tagmanager onto a site",
  "version": "2.12.0-next.0",
  "author": "Whitespace AB <dev@whitespace.se>",
  "dependencies": {
    "@babel/runtime": "^7.12.5"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.1",
    "@babel/core": "^7.12.3",
    "babel-preset-gatsby-package": "^0.13.0-next.0",
    "cross-env": "^7.0.3",
    "gatsby-plugin-utils": "^0.10.0-next.0"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "matomo",
    "matomo tagmanager"
  ],
  "license": "MIT",
  "main": "index.js",
  "peerDependencies": {
    "gatsby": "^2.0.0"
  },
  "scripts": {
    "build": "babel src --out-dir . --ignore \"**/__tests__\"",
    "prepare": "cross-env NODE_ENV=production npm run build",
    "watch": "babel -w src --out-dir . --ignore \"**/__tests__\""
  },
  "engines": {
    "node": ">=10.13.0"
  }
}
